#include <iostream>

using namespace std;
int main() {
    long long a, b, c;
    long long result;

    cin >> a;
    cin >> b;
    cin >> c;

    result = a + b + c;
    cout << result << endl;

    return 0;
}